{
  "name": "talking-stickies-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "npm run start:electron",
    "start:electron": "AGENTS=2 BOOTSTRAP_PORT=$(port) SIGNAL_PORT=$BOOTSTRAP_PORT npm run network:electron",
    "dev": "VITE_ADMIN_PORT=$(port) VITE_APP_PORT=$(port) SIGNAL_PORT=$(port) BOOTSTRAP_PORT=$(port) UI_PORT=8888 npm run x",
    "x": "hc s clean &&  npm run build:happ && UI_PORT=8888 concurrently \"npm start -w ui\" \"npm run start:happ\" \"hc run-local-services -b $BOOTSTRAP_PORT -s $SIGNAL_PORT\"",
    "network:electron": "hc s clean && npm run build:happ && INTERNAL_IP=$(internal-ip --ipv4) concurrently -k \"UI_PORT=1420 npm run -w ui start\" \"npm run launch:electron\"",

    "test": "npm run build:zomes && hc app pack workdir --recursive && npm t -w tests",
    "launch:electron": "hc-spin --ui-port 1420 -n $AGENTS  workdir/talking-stickies.happ",

    "start:moss": " UI_PORT=8888 npm run launch:moss",
    "launch:moss": "concurrently \"npm start -w ui\" \"sleep 1 && weave --agent-idx 1 --dev-config we_dev/config.ts\" \"sleep 10 && weave --agent-idx 2 --dev-config we_dev/config.ts\"",

    "package": "npm run build:happ && npm run package -w ui && hc web-app pack workdir --recursive && npm run weave-hash",
    "start:happ": "echo \"starting with admin $VITE_ADMIN_PORT\" && RUST_LOG=warn echo \"pass\" | hc s -f=$VITE_ADMIN_PORT --piped generate workdir/talking-stickies.happ --run=$VITE_APP_PORT -a talking-stickies network -b \"http://127.0.0.1:$BOOTSTRAP_PORT\"  webrtc \"ws://127.0.0.1:$SIGNAL_PORT\"",
    "build:happ": "npm run build:zomes && hc app pack workdir --recursive",
    "build:zomes": "RUSTFLAGS='--cfg getrandom_backend=\"custom\"' CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown",
    "weave-hash":  "weave hash-webhapp 0.14.0 ./workdir/talking-stickies.webhapp"

  },
  "devDependencies": {
    "@theweave/cli": "0.15.6",
    "@holochain/hc-spin": "^0.600.0",
    "@holochain-playground/cli": "^0.300.1",
    "@rollup/plugin-typescript": "^8.0.0",
    "@tsconfig/svelte": "^2.0.0",
    "bestzip": "^2.2.0",
    "internal-ip": "^7.0.0",
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "json-parse-better-errors": "^1.0.2",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2",
    "svelte-check": "^2.0.0",
    "svelte-preprocess": "^4.10.7",
    "tslib": "^2.0.0",
    "typescript": "^4.0.0",
    "internal-ip-cli": "^2.0.0"
  },
  "engines": {
    "npm": ">=7.0.0"
  },
  "dependencies": {
    "update": "^0.7.4"
  }
}
